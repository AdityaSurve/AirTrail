/** Must match backend `MONITORED_POLLUTANTS` and `pollution_observations.pollutant` strings. */
export const POLLUTANT_IDS = ['PM2.5', 'PM10', 'SO2', 'NO', 'NO2', 'O3', 'CO']

export const DEFAULT_POLLUTANT = 'PM2.5'
